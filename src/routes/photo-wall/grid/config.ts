export const GRID_COLS = 100;
export const GRID_ROWS = 100;

// Zoom limits (multiplier over base cell size)
// e.g. MIN_ZOOM = 3.5 means max zoom-out is 350% of base cell (64px)
export const MIN_ZOOM = 2.5;
export const MAX_ZOOM = 10;
